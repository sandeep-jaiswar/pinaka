# Screening API Service (Placeholder)

This service will expose:
- screener query APIs
- factor metadata endpoints
- watchlist and alert APIs

It will consume curated Gold tables and precomputed features.
