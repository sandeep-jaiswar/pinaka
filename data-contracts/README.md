# Data Contracts

Store dataset contracts here (JSON schema, Avro, Proto, or plain YAML specs).

Suggested structure:
- `raw/` source payload contracts
- `bronze/` normalized contracts
- `gold/` API-facing contracts
